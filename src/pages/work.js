import React from "react";
import Layout from "../components/layout";

const Work = props => (
    <Layout>
      <div>
      <div>Work goes here</div>
      </div>
    </Layout>
);

export default Work;
